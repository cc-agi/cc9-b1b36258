/**
 * Shared environment variable names for MCP.
 * Centralized to prevent drift between crypto code and status panels.
 */
export const MCP_TOKEN_ENC_KEY_NAME = "MCP_TOKEN_ENC_KEY" as const;
export const LOVABLE_API_KEY_NAME = "LOVABLE_API_KEY" as const;
